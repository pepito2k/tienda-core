= form_for [@order, Tienda::Payment.new(:amount => @order.balance)] do |f|
  %dl.text
    %dt= f.label :method, t('tienda.orders.method')
    %dd= f.text_field :method, :class => 'text'
  %dl.text
    %dt= f.label :reference, t('tienda.orders.reference')
    %dd= f.text_field :reference, :class => 'text'
  %dl.amount
    %dt= f.label :amount, t('tienda.orders.amount')
    %dd
      .moneyInput
        .currency= Tienda.settings.currency_unit.html_safe
        = f.text_field :amount, :class => 'text'
  %dl.submit
    %dd= f.submit t('tienda.orders.add_payment') , :class => 'button green'
